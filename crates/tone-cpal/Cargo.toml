[package]
name = "tone-cpal"
description = "cpal audio backend for tone-rs"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
tone-core = { path = "../tone-core" }
cpal = "0.15"
